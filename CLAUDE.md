# CLAUDE.md

クマ監視検知システム。作業前に必ず `docs/SPEC.md` を読むこと。

---

## このプロジェクトの前提

住宅街および里山で春〜秋・早朝〜夕方にツキノワグマを検知し、**猟友会および警察へ通報する**システム。住民向けの即時警報ではない。

処理は端末とサーバのカスケード。

```
端末   S1 背景差分 → S2 トラック生成 → S3 ROI分類器 → 投機的録画 → アップロード
サーバ S4 静止画の即時判定 → S5 映像の事後判定 → 人間確認キュー → 通報
```

---

## 最優先の原則

### 1. 適合率がすべてに優先する

通報先が専門機関なので、誤報を数回出せば運用が止まる。再現率は後から上げられるが、失った信頼は戻らない。閾値やモデルの選択で迷ったら、**適合率を取る**。

### 2. `docs/SPEC.md` を常に最新の真実に保つ

実装中に判明した事実（特に §3 の端末素性）は、コードを書いたらすぐ SPEC.md に反映する。SPEC とコードが食い違ったら SPEC を直す。

### 3. 端末が壊れたら現地に行くしかない

山や農地に設置された端末にはリモートログインできない。以下は破ってはいけない。

- **設定は必ず検証してから適用する**（`shared/config_schema.json`）
- **試用期間内に確認が取れなければ自動で前の設定に戻す**
- **LKG（最後に成功した設定）は STABLE 状態でしか更新しない**
- **一度ロールバックした etag は再受理しない**
- **`safety.temp_shutdown_c` は端末側で強制クランプする。サーバから無効化できてはならない**

これらのロジックは `reference/config_rollback_reference.py` に検証済みの参照実装がある。Kotlin へはこれをそのまま移植する。テストは `tests/test_config.py`。

### 4. モデルは BSD / Apache 系のみ

**Ultralytics（AGPL-3.0）を導入しないこと。** 将来 自治体や猟友会へ提供する可能性があり、AGPL はネットワーク越しの提供でもソース開示義務が生じる。

- S3（端末 96×96 分類器）: torchvision `mobilenet_v3_small` → ONNX または `ai-edge-torch` 経由で TFLite
- S4（サーバ即時判定）: torchvision `fasterrcnn_resnet50_fpn_v2`
- S5（映像事後判定）: torchvision バックボーン + 自前の系列分類器

`pip install ultralytics` を提案しないこと。COCO 事前学習のクラス集合は同じなので、初年度の「動物クラスの和集合」運用は torchvision でそのまま成立する。

### 5. 撮影データと座標をコミットしない

画像・映像・イベントデータには通行人や車両が写り込む。設置地点の座標を含む校正ファイルも同様。`.gitignore` で除外済みだが、**新しい出力先を追加したら `.gitignore` も更新すること。**

コミット前に `git status` を確認する。1度でも入ると履歴から消すのが面倒になる。

### 6. 通報の失敗を握り潰さない

通報が届かなかったことに気づけないのが最悪の事故。`NotifierChain` の `failure_hook` は必ず実装し、管理者通知とローカル記録に繋ぐ。

---

## 単一の正となるファイル

| ファイル | 役割 | 注意 |
|---|---|---|
| `docs/SPEC.md` | 要件・設計・検証計画 | 実装中に更新する |
| `shared/config_schema.json` | 設定パラメータの定義 | **サーバと端末が同じものを読む。ここを二重管理すると、サーバが通した設定を端末が拒否して永久にロールバックする** |

設定パラメータを追加・変更するときは、必ず `config_schema.json` を編集してから両側を直す。ハードコードしない。

---

## 技術的な制約

### 端末（Android）

- **minSdk 24（Android 7.0）。** MediaPad T3 8 / Snapdragon 425 / RAM 2GB
- **NNAPI は使えない**（API 27 以降）。TFLite は XNNPACK の CPU デリゲート固定
- **Camera2 のハードウェアレベルが LEGACY の可能性がある。** その場合は旧 Camera1 API へフォールバックする。判明するまで両対応を前提に書く
- **前後カメラの同時使用はしない。** 交互に開閉する
- **EMUI の「保護されたアプリ」「起動管理」への登録が必要。** 手順を README に書く
- **`network_security_config` に自前 CA を同梱する。** Android 7.0 のルート証明書ストアは古い
- 状態の永続化は一時ファイル経由の置換（`File.renameTo`）で行う。書き込み中の電源断でロールバック状態が壊れると仕組み全体が無意味になる

### サーバ

- Python 3.11 / FastAPI / SQLite（台数が増えたら PostgreSQL）
- **推論は torchvision（BSD-3-Clause）のみ。Ultralytics YOLO は AGPL-3.0 なので使わない**
- **端末にプッシュできない前提。** コマンドはハートビート応答で渡すポーリング方式
- 人間確認キューの UI は**スマホで操作できること**

---

## 守るべき規約

### 時刻

**保存はすべて UTC。表示のみ JST。** イベントには `clock_offset_ms`（NTP との差）を必ず記録する。トラック速度の計算とイベント統合がこれに依存する。

### 秘密情報と実データ

以下は**絶対にコミットしない**。`.gitignore` を確認すること。

- SMTP パスワード、端末トークン、API キー（`.env` に置く）
- 撮影画像・映像・イベントデータ（人物が写り込む。個人情報）
- 設置地点の座標を含む校正ファイル

### 誤検知の記録

誤検知は必ず原因コード（`WIND_VEG` / `CLOUD` / `RAIN` / `INSECT` / `VEHICLE` / `PERSON` / `BIRD` / `EXPOSURE` / `LENS` / `UNKNOWN`）付きで記録する。これがないと改善対象が決められない。

### メトリクス

機能を実装したら、**同時に** `docs/SPEC.md` §12 のカウンタを入れる。後付けすると必ず漏れ、検証計画の KPI が測れなくなる。

特に `s2_rejected_by_{filter}`（どのフィルタが何を落としたか）は必須。

---

## 評価

`server/eval/metrics.py` を使う。

```bash
python server/eval/metrics.py events.jsonl --s5 0.75
python server/eval/metrics.py events.jsonl --known-positives 120   # 真の再現率
```

**Accuracy を単独で判断材料にしない。** クマは全イベントの数%しかないため、「全部クマでない」と答えるだけで 97% を超える。Balanced Accuracy と Precision を見る。

サーバのデータだけで計算した Recall は**条件付き再現率**であり真の値ではない。S1/S2 が落としたものは分母に入らない。真の再現率には Phase 1 の距離別試験による独立した正解数が要る。

---

## テスト

```bash
python tests/test_config.py        # 値域チェックとロールバック（38件）
```

**設定検証とロールバックのテストは絶対に壊さないこと。** ここが壊れると端末が文鎮化して回収に行くことになる。

Phase 1 で撮影した映像は正解つきの回帰テストセットとして保存する。閾値やモデルを変えたら必ずこれを流す。

---

## 開発の順序

`docs/SPEC.md` §14 のマイルストーンに従う。

### 試験モード（§9.5）は Phase 1 の前提

通常運用では S1 が落とした候補の記録が残らないため、距離別試験で「検知しなかった」ことしか分からない。`stage_trace` モードで**各候補がどの段のどのフィルタで落ちたか**を記録すること。これがないと Phase 1 が実務的に回らない。

時間上限とストレージ上限で**必ず自動終了**させる。両者は hard clamp 対象。

**M0（`tools/device_probe`）が最優先。** 端末の素性が確定するまで、他の設計判断はすべて仮のもの。特に露出ロックの可否とプレビュー最大解像度は、結果によって設計が根本から変わる。

`tools/replay.py` と `tools/fake_device.py` は早期に作る。前者は録画から検知パイプラインを再実行するもので、閾値調整のたびに現地へ行く必要をなくす。後者は端末なしでサーバを開発するためのシミュレータ。

---

## 実装しないこと

- 夜間監視（端末の IR カットフィルタにより原理的に不可）
- パノラマ合成（前後カメラに重複がなく不要）
- 三角測量（単一端末のため方位＋推定距離のみ）
- 住民向けの即時警報
